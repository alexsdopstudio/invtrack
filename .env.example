# --- Database -------------------------------------------------------------
# Supabase (production): use the Session pooler connection string from
# Supabase Dashboard -> Project Settings -> Database, with the psycopg driver:
#   DATABASE_URL=postgresql+psycopg://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:5432/postgres
# Local development: leave unset to use a SQLite file (backend/invtrack.db).
#DATABASE_URL=postgresql+psycopg://postgres.xxxx:password@aws-0-eu-central-1.pooler.supabase.com:5432/postgres

# --- SEC EDGAR ------------------------------------------------------------
# The SEC asks automated clients to identify themselves. Use your name/app
# and a real contact email.
SEC_EDGAR_USER_AGENT="invtrack personal research you@example.com"

# --- Congressional trade datasets ------------------------------------------
# Senate trades come from the official efdsearch.senate.gov (no key needed).
# House trades expect Stock Watcher-shaped JSON; the original bucket is
# defunct (July 2026) — point this at a live mirror if you find one.
#HOUSE_DATA_URL=https://house-stock-watcher-data.s3-us-west-2.amazonaws.com/data/all_transactions.json

# --- AI filing analysis (optional, PAID) ------------------------------------
# Set a Claude API key to enable the "Analyze latest 10-K" feature. Each new
# filing analyzed costs roughly $0.25-0.50 (Opus 4.8); reports are cached per
# filing. Leave unset to keep InvTrack 100% free.
#ANTHROPIC_API_KEY=sk-ant-...
#ANALYSIS_MODEL=claude-opus-4-8

# --- Auto-refresh & alerts ---------------------------------------------------
# Daily background ingestion + rescore + alert detection (on by default).
#AUTO_REFRESH_ENABLED=true
#AUTO_REFRESH_HOUR=7
# Optional alert push. Any webhook that accepts JSON works: Slack incoming
# webhooks (reads "text"), Discord webhooks (reads "content"), ntfy topics,
# or your own receiver. Leave unset for in-app alerts only.
#ALERT_WEBHOOK_URL=https://discord.com/api/webhooks/...

# --- CORS -----------------------------------------------------------------
# Comma-separated origins allowed to call the API (the Vite dev server by default).
#CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
